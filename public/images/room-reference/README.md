# Room Reference

Place the supplied room reference image here as:

```txt
stranger-room-reference.png
```

The React scene uses this raster image directly and only overlays the CRT screen interaction area.
